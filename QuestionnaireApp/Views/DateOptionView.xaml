<UserControl x:Class="QuestionnaireApp.Views.DateOptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:QuestionnaireApp.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=viewModels:OptionViewModel}">

	<StackPanel>
		<DatePicker SelectedDate="{Binding Content}" Width="200" HorizontalAlignment="Left"
		            SelectedDateChanged="DatePicker_OnSelectedDateChanged" />
	</StackPanel>
</UserControl>